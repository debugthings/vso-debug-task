/// <reference path="../../definitions/vsts-task-lib.d.ts" />
const http = require("http");
const spawn = require('child_process').spawn;
const cnt = require('./controllers/process.ts')

class Server {
    constructor (port)
    {
        
    }
}